{
  "name": "ec2script",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "npx esbuild . --bundle --minify --platform=node --outfile=deploy/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.1",
    "got": "^14.2.0",
    "node-schedule": "^2.1.1"
  },
  "devDependencies": {
    "@types/got": "^9.6.12",
    "@types/node-schedule": "^2.1.6",
    "esbuild": "^0.20.0"
  }
}
